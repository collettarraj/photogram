<div class="card">
  <div class="card-header d-flex align-items-center justify-content-between">
    Usersbests

    <a data-toggle="collapse" href="#user_usersbests_new_form">
      Add usersbest
    </a>
  </div>

  <ul class="list-group list-group-flush">
    <% @user.usersbests.each do |usersbest| %>
      <li class="list-group-item d-flex align-items-center justify-content-between">
        <%= link_to usersbest, usersbest %>

      </li>
    <% end %>

    <div class="card-footer">
      <div class="collapse" id="user_usersbests_new_form">
        <%= render partial: "users/user_usersbests_new_form" %>
      </div>
    </div>

  </ul>
</div>
